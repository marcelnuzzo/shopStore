{#
{% extends 'base.html.twig' %}

{% form_theme formUtilisateur 'bootstrap_4_layout.html.twig' %}

{% block body %}
    <div class="jumbotron" style="background-color:DeepSkyBlue">
    {% if editMode %}
        <h1 style ="text-align:center">Modification de l'utilisateur</h1>                
    {% else %}
        <h1 style ="text-align:center">Création d'un utilisateur</h1>
    </div>
    {% endif %}
        {{ form_start(formUtilisateur) }}

        {{ form_raw(formUtilisateur.nom, {'attr': {'placeholder': "Nom de l' utilisateur"}}) }}
        {{ form_raw(formUtilisateur.prenom, {'attr': {'placeholder': "Prénom de l'utilisateur"}}) }}
        {{ form_raw(formUtilisateur.date_de_naissance, {'attr': {'placeholder': "Date de naissance de l'utilisateur"}}) }}
        {{ form_raw(formUtilisateur.login, {'attr': {'placeholder': "Login de l'utilisateur"}}) }}
        {{ form_raw(formUtilisateur.mot_de_passe, {'attr': {'placeholder': "Mot de passe de l'utilisateur"}}) }}
        {{ form_raw(formUtilisateur.date_de_location, {'attr': {'placeholder': "date de location"}}) }}
        {{ form_raw(formUtilisateur.duree, {'attr': {'placeholder': "Durée de la location"}}) }}
        {{ form_raw(formUtilisateur.fin_de_location, {'attr': {'placeholder': "Fin de la location"}}) }}
        {{ form_raw(formUtilisateur.mail, {'attr': {'placeholder': "Mail de l'utilisateur"}}) }}

        <button type="submit" class="btn btn-success">
            {% if editMode %}
                Modifier
            {% else %}
                Ajouter
            {% endif %}
        </button>
        
        {{ form_end(formUtilisateur) }}
    
{% endblock %}
#}
{% extends 'base.html.twig' %}

{% form_theme formUtilisateur 'bootstrap_4_layout.html.twig' %}

{% block body %}
    <div class="jumbotron" style="background-color:DeepSkyBlue">
    {% if editMode %}
        <h1 style ="text-align:center">Modification de l'utilisateur</h1>                
    {% else %}
        <h1 style ="text-align:center">Création d'un utilisateur</h1>
    </div>
    {% endif %}
        {{ form_start(formUtilisateur) }}

        {{ form_row(formUtilisateur.nom, {'attr': {'placeholder': "Nom de l' utilisateur"}}) }}
        {{ form_row(formUtilisateur.prenom, {'attr': {'placeholder': "Prénom de l'utilisateur"}}) }}
        {{ form_row(formUtilisateur.date_de_naissance, {'attr': {'placeholder': "Date de naissance de l'utilisateur"}}) }}
        {{ form_row(formUtilisateur.login, {'attr': {'placeholder': "Login de l'utilisateur"}}) }}
        {{ form_row(formUtilisateur.mot_de_passe, {'attr': {'placeholder': "Mot de passe de l'utilisateur"}}) }}
        {{ form_row(formUtilisateur.date_de_location, {'attr': {'placeholder': "date de location"}}) }}
        {{ form_row(formUtilisateur.duree, {'attr': {'placeholder': "Durée de la location"}}) }}
        {{ form_row(formUtilisateur.fin_de_location, {'attr': {'placeholder': "Fin de la location"}}) }}
        {{ form_row(formUtilisateur.mail, {'attr': {'placeholder': "Mail de l'utilisateur"}}) }}

        <button type="submit" class="btn btn-success">
            {% if editMode %}
                Modifier
            {% else %}
                Ajouter
            {% endif %}
        </button>

        {{ form_end(formUtilisateur) }}
    
{% endblock %}